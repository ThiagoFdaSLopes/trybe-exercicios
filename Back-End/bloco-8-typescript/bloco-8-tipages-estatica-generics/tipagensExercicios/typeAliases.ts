type Bird = {
  name: string,
  age: number,
};

type Values = {
  numberA: number,
  numberB: number,
};

type Address = {
  address: string,
  number: number,
  postalCode: number,
  state: string,
  city: string,
};